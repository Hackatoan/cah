# Copy to .env (gitignored) and fill in real values before `docker compose up`.
# Shared leaderboard Postgres connection string. Leave unset to run with the
# leaderboard disabled (server.js / db.js fail soft with no DATABASE_URL).
DATABASE_URL=postgresql://games:CHANGE_ME@games-db:5432/games
